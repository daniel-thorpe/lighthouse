# The Worker name must stay "lighthouse": it is what gives the site its
# workers.dev hostname, so renaming it would change the live URL.
name = "lighthouse"
main = ".open-next/worker.js"
compatibility_date = "2026-09-01"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = ".open-next/assets"
binding = "ASSETS"
